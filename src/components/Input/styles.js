import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: 300,
    height: 40,
    borderWidth: 1,
    borderColor: "black",
    borderRadius: 8,
    margin: 20,
    padding: 4,
  },
});

export default styles;
